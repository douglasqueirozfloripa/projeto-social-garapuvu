{
  "name": "freelavalia-360",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "FreelaAvalia 360 — pacote de deploy (frontend Vite + backend fake como função serverless na Vercel)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
